pandas>=2.2
numpy>=1.26
SQLAlchemy>=2.0
psycopg2-binary>=2.9
scikit-learn>=1.4
streamlit>=1.35
matplotlib>=3.8
python-dotenv>=1.0

# Added for Stage 3 (Kafka MVI) and Stage 6 (drift detection) --
# previously missing, which is why CI's unit-test step failed with
# ModuleNotFoundError: No module named 'confluent_kafka' when it tried
# to import kafka_consumer_etl.py.
confluent-kafka>=2.3
prometheus-client>=0.20

# scipy and joblib were already present transitively (scikit-learn
# depends on both), which is why drift_detection.py imported fine
# without them listed -- pinning explicitly so that stays true even
# if scikit-learn's own dependencies ever change.
scipy>=1.11
joblib>=1.3

# Stage 7 (SHAP explainability)
shap>=0.45
pytest 
mlflow 
optuna 
scikit-image
pyarrow
pytesseract
